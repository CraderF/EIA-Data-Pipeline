# EIA-Data-Pipeline
This project is a simple ETL pipeline that pulls monthly electricity retail sales data from the U.S. Energy Information Administration (EIA) API. It processes the data into a clean, structured format and uploads both the raw and cleaned datasets to an AWS S3 data lake. 
